#include "main.h"

/**
 * set_string - a function that sets value of pointer to char
 * @s: the pointer that is to be set to char
 * @to: the car string to be pointed
 *
 * Return: returns void
 */

void set_string(char **s, char *to)
{
	*s = to;
}
