#include "main.h"
#include <string.h>

/**
 * print_rev - prints a string in reverse
 * @s: the string that is printed
 */

void print_rev(char *s)
{
	int n;
	int a;

	n = 0;
	while (s[n] != '\0')
		n++

	for (a = n - 1; a >= 0; a--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
