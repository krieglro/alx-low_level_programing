#include "main.h"

/**
 * _islower -> checks if a characcter is lowercase
 * @c: character argument
 * Return: returns 1 and 0 depending on condition
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
