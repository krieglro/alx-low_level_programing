#include "main.h"
/**
 * print_last_digit -> prints last digit
 * @n: argument passed
 * Return: last digit
 */
int print_last_digit(int n)
{
	int a;

	if (n < 0)
		n = -n;
	a = n % 10;
	_putchar(x + '0');
	return (n % 10);
}
