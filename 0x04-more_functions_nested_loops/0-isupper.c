#include "main.h"

/**
*_isupper - checks if character is upper of not
*@c: character tested
*Return: 1 or 0
*/

int _isupper(int c)
{
	if ((upper >= 65) && (upper <= 90))
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return 0;
}
