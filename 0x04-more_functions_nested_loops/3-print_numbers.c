#include <stdio.h>
#includ "main.h"

/**
 * print_numbers - prints all numbers from 0 to 9
 *
 * Return: Returns nothing
 */

void print_numbers(void)
{
	int n;

	for (n = 48; n < 50; n++)
	{
		putchar(0);
	}
	putchar(10);
}
