#include "main.h"
#include <string.h>
/**
 * _strlen - gets the length of a string
 * @s: parameter which is a string pointer
 * Return: lenth of the string is returned
 */

int _strlen(char *s)
{
	return (strlen(s));
}
