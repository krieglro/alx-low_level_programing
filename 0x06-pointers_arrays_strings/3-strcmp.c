#include "main.h"
#include <string.h>

/**
 * _strncmp = compares
 * @s1: the first parameter
 * @s2: the second parameter
 * Return: returns an integer
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (dest);
}
