#include <stdio.h>
#include <stdlib.h>

/*
 * main combinations of a number 
 */

int main(void)
{ 
	
	int n = 48;
	while (n <= 1020)
	{
		putchar(n);
		if (n == 57)
			n += 39
		++n;
	}

	putchar('\n');

	return(0);
}
